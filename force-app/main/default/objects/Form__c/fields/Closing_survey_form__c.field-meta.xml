<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Closing_survey_form__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>The survey form (if any) that online apps instantiates and presents to the grantee upon submission of this form.  Currently only used for final yearly report forms.</description>
    <externalId>false</externalId>
    <inlineHelpText>The survey form (if any) that online apps instantiates and presents to the grantee upon submission of this form.  Currently only used for final yearly report forms.</inlineHelpText>
    <label>Closing survey form</label>
    <referenceTo>Form__c</referenceTo>
    <relationshipLabel>Forms</relationshipLabel>
    <relationshipName>Forms</relationshipName>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
