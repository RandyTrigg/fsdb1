<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>LinkedAccountContactClash</fullName>
    <active>true</active>
    <description>This advisor links to a contact record, so its parent account must be the same as the advisor&apos;s linked account.</description>
    <errorConditionFormula>AND(
 NOT(ISBLANK( Contact__c )),
 Contact__r.AccountId &lt;&gt; Account__c
)</errorConditionFormula>
    <errorMessage>This advisor links to a contact record, so its parent account must be the same as the advisor&apos;s linked account.</errorMessage>
</ValidationRule>
